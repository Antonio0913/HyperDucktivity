import React from "react";
import { Link } from "react-router-dom";

const Settings = () => {
  return (
    <div>
      <h1>(Placeholder Page) Todo Settings</h1>

      <nav>
        <Link to="/">Back to Home</Link>
      </nav>
    </div>
  );
};

export default Settings;
